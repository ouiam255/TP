# Server configuration
server.port=8081
server.host=0.0.0.0

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=benchmark
DB_USER=benchmark
DB_PASSWORD=benchmark

# Performance mode: baseline (N+1 queries) or optimized (JOIN FETCH)
query.mode=optimized
